{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "fp=$(jq -r '.tool_input.file_path // empty'); case \"$fp\" in */packages/web/*) out=$(cd \"$CLAUDE_PROJECT_DIR/packages/web\" && bun vp check --fix \"$fp\" 2>&1) || { echo \"$out\"; exit 2; } ;; esac"
          }
        ]
      }
    ]
  }
}
